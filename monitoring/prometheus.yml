global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: codeveira
    metrics_path: /metrics
    scheme: http
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/token
    static_configs:
      - targets: ['app:3000']
